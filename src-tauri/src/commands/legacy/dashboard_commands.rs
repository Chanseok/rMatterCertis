//! Legacy dashboard commands (placeholder)
//! This module remains behind the `legacy-ui` feature. We keep only the minimal state stub.

#[derive(Debug, Default, Clone)]
pub struct DashboardServiceState;

// No commands are exposed for legacy dashboard in current builds.
// This stub exists to satisfy `.manage(DashboardServiceState::default())` when the feature is enabled.
