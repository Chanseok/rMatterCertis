#![cfg(any())]
//! ARCHIVED: infrastructure/matter_crawler.rs (legacy Matter crawler)
// Historical snapshot preserved for reference. Not part of active build.

/*
use anyhow::{Context, Result};
use scraper::{Html, Selector};
use std::sync::Arc;
use sqlx::SqlitePool;
use tokio::sync::Mutex;
use tracing::{debug, error, info, warn};
use uuid::Uuid;

use crate::domain::matter_product::{MatterProduct, MatterCrawlerConfig, MatterCrawlingSession, CrawlingStage};
use crate::infrastructure::http_client::{HttpClient, HttpClientConfig};
use crate::infrastructure::matter_product_repository::MatterProductRepository;

pub struct MatterProductsCrawler { /* ...full content preserved... */ }
*/
