#![cfg(any())]
//! ARCHIVED: legacy Matter crawler (see `_archive/infrastructure/matter_crawler.rs`)
// Not part of the active build path. Prefer integrated repositories + crawl_engine services.

// intentionally empty
