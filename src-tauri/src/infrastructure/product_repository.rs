#![cfg(any())]
//! ARCHIVED: product repository (previously tracked under `_archive/infrastructure/product_repository.rs`; see scripts/_backups)
// Not part of the active build.

// intentionally empty
