#![cfg(any())]
//! ARCHIVED: matter product repository (historical reference from `_archive/infrastructure/matter_product_repository.rs`; archives relocated to scripts/_backups)
// Not part of the active build.

// intentionally empty
